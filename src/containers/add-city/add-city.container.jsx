import React from 'react';


const AddCityContainer = (props) => {
    return (
        <div className="addcity-container">
            Add New City
        </div>
    );
};

export default AddCityContainer;